<a href="{{ route('home') }}" class="active-link">{{ __('messages.navHome') }}</a>
<a href="#">{{ __('messages.navProducts') }}</a>
<a href="{{ route('login') }}">{{ __('messages.navLogin') }}</a>
<a href="{{ route('register') }}">{{ __('messages.navRegister') }}</a>
